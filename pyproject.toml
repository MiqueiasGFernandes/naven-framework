[project]
name = "naven"
version = "0.1.0"
description = "A production-first HTTP framework for Python"
requires-python = ">=3.11"

dependencies = [
]

[dependency-groups]
dev = [
    "pre-commit>=4.6.2",
    "pytest>=9.1.1",
    "ruff>=0.16.8",
    "uvicorn>=0.30",
]
